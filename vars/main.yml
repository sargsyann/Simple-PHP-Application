apt_packages:
  - apt-transport-https
  - ca-certificates
  - curl
  - gnupg-agent
  - software-properties-common
  - python3
  - python3-dev
  - python3-pip
  - python3-setuptools
  - python3-docker

python_packages:
  - docker
  # - docker-compose

docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io

ansible_collection:
  - ansible-galaxy collection install community.general
  - ansible-galaxy collection install community.docker

files:
  - data
  - nginx.default.conf
  - Dockerfile

app_target_dir: /var/mytask/